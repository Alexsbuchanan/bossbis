# BossBiS v0.1.3 — Ranged + Magic Max Hit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Port the player **ranged** (`getPlayerMaxRangedHit`) and **magic** (`getPlayerMaxMagicHit`) max-hit methods, completing `getMinAndMax` for all three styles. The 2 seeded **magic `maxHit`** rows light up and pass exactly (Tumeken's Shadow 65, fire-bolt-chaos-gauntlets 20); add **ranged `maxHit`** coverage rows. Distribution/DPS/TTK stay stubbed (v0.1.4).

**Architecture:** Extend the existing `PlayerVsNpcCalc` (v0.1.2 has melee max hit, `getMinAndMax` melee live, `getMaxHit()`). Fill in the ranged + magic branches of `getMinAndMax` (currently throwing). Magic uses `getSpellMaxHit` (port into `Spell`/a helper, using the existing `SpellRepository.byName` for elemental tier lookups). Gate = the parity corpus (real weirdgloop values).

**Conventions:** identical to the v0.1.1/v0.1.2 plans — PORT-style, `strictfp`, **no RNG**, `long` in `trackFactor`, `Math.trunc`→`(int)`/`(long)`, faithful-first, per-task TDD + `port: … <weirdgloop@5e313c1>` commits, no push. JDK 17 → Java 11. **WSL2 quirk:** run Gradle with `--no-daemon --max-workers=1` (intermittent "Failed to exec spawn helper" otherwise). Upstream source at `tools/upstream/src/`.

**Working directory:** `/home/alexb/runelite/projects/bossbis` (on `main` at tag `v0.1.2`). **Create branch `feat/v0.1.3-ranged-magic-maxhit` first.**

**The distribution-vs-base-max rule (carried from v0.1.2 — critical):** `getMaxHit()` = `getMinAndMax().max()` captures only BASE max-hit logic. **Bonuses applied in the distribution pipeline are NOT here** — for RANGED that means **bolt enchant procs** (ruby/diamond/etc. — `dists/bolts.ts`) and any reroll/min-hit transforms; for MAGIC, spell procs handled in `getDistribution`. When choosing corpus rows, pick cases whose upstream `maxHit` is a pure base value (no bolt proc, no distribution transform). If a transcribed row's value can't be reproduced by `getMaxHit()` because it's distribution-side, gate it to v0.1.4 (tag `PlayerVsNpcCalc.distribution`) rather than faking it — exactly as blisterwood was handled.

**Definition of done:** `./gradlew build` green; the 2 magic `maxHit` rows pass exactly (65, 20); ≥3 ranged `maxHit` coverage rows pass; all prior rows (melee maxHit, attack/defence rolls, hit chance) still pass; `PortStatusTest` green; determinism OK; jar <10 MiB; `PlayerVsNpcCalc.maxRangedHit` + `maxMagicHit` → `ported`, distribution/DPS still `not-ported`; CI green on `main`; tag `v0.1.3`.

---

# Milestone 1 — Ranged max hit

## Task 1.1: `getPlayerMaxRangedHit` + ranged corpus rows

**Files:** Modify `src/main/java/org/bossbis/calc/PlayerVsNpcCalc.java`; add ranged `tools/scenarios/0NN-*.json`; Test `src/test/java/org/bossbis/calc/RangedMaxHitTest.java`.
**Source:** `PlayerVsNPCCalc.ts:656-848` `getPlayerMaxRangedHit`. Port faithfully in order:
- effective level = `skills.ranged + boosts.ranged` (or `skills.str + boosts.str` for Eclipse atlatl / Hunter's spear); Holy water early-return path; special-attack MSB/MLB/Seercull/Ogre-bow early-return path (`+10`, ammo-only); combat prayers `factorStrength` (Sharp Eye force-1 case); stance `Accurate +3`; ranged void `[11,10]`; `gearBonus = bonuses.ranged_str + 64`; `baseMax = trackMaxHitFromEffective(...)`; then the ordered ranged damage multipliers (crystal armour+bow, avarice/forinthry, salve(ei)/(i), slayer/imbued-black-mask, **Twisted bow** scaling, dragon hunter crossbow, rev weapon, Scorching bow demonbane, chinchompa, etc. — every branch through the end of the method).
**Gotchas:** Twisted bow max-hit scaling depends on monster magic level (cap rules) — port verbatim. Drop leagues branches (spec §4.2). `long` in `trackFactor`. **Do NOT** include bolt-proc damage (that's `dists/bolts.ts`, distribution-side, v0.1.4).
**Corpus rows (pick CLEAN base-max cases, no bolt procs):** transcribe ranged `maxHit` cases from `tools/upstream/src/tests/calc/GeneratedTests.test.ts` (search `expect(maxHit)` with ranged loadouts) — good candidates: a **Twisted bow** case, a **Toxic blowpipe** case, a **chinchompa** case, a **crystal bow + crystal armour** case. Each: `exercises: ["PlayerVsNpcCalc.maxRangedHit"]`, `expected: {"maxHit": <verbatim>}`. After adding each, RUN it: keep if `getMaxHit()` matches; if it doesn't because the case has a bolt proc / distribution effect, DROP it and pick a cleaner case (do not fake). Flip `PlayerVsNpcCalc.maxRangedHit` → `ported`; update `PortStatusTest`.

- [ ] **Step 1:** Failing `RangedMaxHitTest` with a known value (e.g. a tbow or blowpipe case → its upstream maxHit). 
- [ ] **Step 2:** FAIL → **Step 3:** un-stub `getMinAndMax` ranged branch + port `getPlayerMaxRangedHit`; add ≥3 clean ranged corpus rows; flip port-state. 
- [ ] **Step 4:** Run corpus → ranged rows PASS exactly; all prior rows still pass; determinism OK; build green. 
- [ ] **Step 5: Commit** `port: getPlayerMaxRangedHit + ranged max-hit corpus rows <weirdgloop@5e313c1>`; `PORT_STATE.md` `PlayerVsNpcCalc` → `partial: accuracy + melee/ranged max hit`.

---

# Milestone 2 — Magic max hit

## Task 2.1: `getSpellMaxHit` + `getPlayerMaxMagicHit` + the 2 magic rows light up

**Files:** Modify `PlayerVsNpcCalc.java` (+ a `getSpellMaxHit` helper, in `Spell` or the calc, using `SpellRepository.byName`); Test `src/test/java/org/bossbis/calc/MagicMaxHitTest.java`; `port-state.json`; `PortStatusTest`.
**Source:**
- `tools/upstream/src/types/Spell.ts:26+` `getSpellMaxHit(spell, magicLevel)` — returns `spell.max_hit`, with the elemental strike/bolt/blast/wave/surge tiers resolved by magic level via `spellByName(...)` (use `SpellRepository.byName`). Port the tier thresholds verbatim.
- `PlayerVsNPCCalc.ts:960-1100+` `getPlayerMaxMagicHit`: base maxHit from `getSpellMaxHit(spell, magicLevel)` (Magic Dart special), ELSE the powered-staff if/else chain (Starter staff 8; Trident-of-seas `max(1, trunc(magicLevel/3-5))`; Thammaron's; Accursed; Trident-of-swamp; Sanguinesti; Dawnbringer; **Tumeken's Shadow** `max(1, trunc(magicLevel/3)+1)`; Eye of ayak; Lithic; Warped; Bone staff; nightmare-staff specs; crystal/corrupted staves; salamanders) — port the WHOLE chain verbatim; then (after `:1028`) the magic-damage-bonus application (the `% from gear` — occult/tome/smoke-staff/virtus/etc., via `player.bonuses.magic_str` and the gear/buff branches) and the magic multipliers (salve, slayer, etc.). Returns `MinMax`.
**Gotchas:** Tumeken's Shadow's ×3/×4 `magic_str` is already in the M1 Equipment aggregation (capped 1000) — the base here is the `magicLevel/3+1` formula; the magic-damage-bonus % uses the aggregated `bonuses.magic_str`. `chaos/god gauntlets` add a flat amount to specific bolt spells (in getSpellMaxHit or the bonus step — port where upstream does). Drop leagues. `long`/trunc discipline.
**Gate (the seeded rows):** un-stub `getMinAndMax` magic branch; the 2 seeded rows **006-fire-bolt-chaos-gauntlets (maxHit=20)** and **005-tumekens-shadow-max-mage (maxHit=65)** PASS EXACTLY. Flip `PlayerVsNpcCalc.maxMagicHit` → `ported`; update `PortStatusTest` (these 2 rows now run). If a value mismatches, fix the port (re-check the powered-staff formula / spell tier / magic-damage-bonus order). Do NOT change expected values. If BLOCKED (value is distribution-side), report with computed-vs-expected.

- [ ] **Step 1:** Failing `MagicMaxHitTest` (`getMaxHit()==65` for the shadow loadout, `==20` for fire-bolt+chaos-gauntlets). 
- [ ] **Step 2:** FAIL → **Step 3:** port `getSpellMaxHit` + `getPlayerMaxMagicHit` + un-stub `getMinAndMax` magic. 
- [ ] **Step 4:** Run corpus → the 2 magic rows PASS exactly; all prior rows pass; build/determinism green. 
- [ ] **Step 5: Commit** `port: getPlayerMaxMagicHit + getSpellMaxHit (2 magic rows green) <weirdgloop@5e313c1>`; `PORT_STATE.md` `PlayerVsNpcCalc` → `partial: accuracy + all-style max hit (no distribution)`.

## Task 2.2: Magic coverage rows

**Files:** add magic `tools/scenarios/0NN-*.json`; regenerate; `PortStatusTest`.
Add ≥2 magic coverage rows for unexercised paths (e.g. a **standard-spell with occult/tome magic-damage-bonus** case, a **Trident/Sanguinesti** powered-staff case) transcribed from `GeneratedTests.test.ts`/`DemonbaneSpells.test.ts`. Same anti-trap rule: keep only rows whose `getMaxHit()` matches (drop distribution-side). Fix any mismatched magic-damage-bonus branch.

- [ ] **Steps 1–3:** add ≥2 magic coverage rows, run, fix any branch, all pass. — [ ] **Step 4: Commit** `test: magic max-hit coverage rows <weirdgloop@5e313c1>`.

---

## Final verification & integration
- [ ] `./gradlew build --no-daemon --max-workers=1` green; all max-hit rows (melee + ranged + magic) pass; attack/defence/hit-chance rows pass; blisterwood + any distribution-side rows still skip; `PortStatusTest` green; unit tests green.
- [ ] determinism OK; jar < 10 MiB.
- [ ] `PORT_STATE.md` + `port-state.json`: `maxRangedHit` + `maxMagicHit` ported; distribution/`getMax`/DPS/TTK not-ported.
- [ ] PR `feat/v0.1.3-ranged-magic-maxhit` → `main`; CI green on PR; merge; CI green on `main`; tag `v0.1.3`; delete branch.

## Out of scope (v0.1.4+)
- `HitDist`/distribution/`getMax` (distribution top)/`getDps`/`getTtk`/`getSpecCalc`; bolt/claw spec distributions (`dists/`); the blisterwood vampyrebane + any other distribution-side bonus. Leagues; full raid stat-scaling.
