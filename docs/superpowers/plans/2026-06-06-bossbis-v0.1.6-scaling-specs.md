# v0.1.6 — raid stat-scaling + spec attacks (the last port piece)

Branch: `feat/v0.1.6-scaling-specs` (do NOT switch). Upstream pin: weirdgloop@5e313c1.
Conventions: PORT-style verbatim, `strictfp`, no RNG, `long` intermediates, `Math.trunc`→cast,
no `new Gson()` in main, Gradle `--no-daemon --max-workers=1`, commits `port: … <weirdgloop@5e313c1>`,
no push.

## Part 1 — raid stat-scaling (un-stub `scaleMonster`)

Port the full chain from `src/lib/MonsterScaling.ts` + `src/lib/scaling/{ChambersOfXeric,
TheatreOfBlood,TombsOfAmascut,Vardorvis,Phases}.ts`:

1. **Constants** — add the missing CoX/ToB id sets to `Constants.java`:
   - `TOB_MONSTER_IDS`, `TOB_EM_MONSTER_IDS` (ToB hp-scaling targets);
   - `OLM_IDS` (set = head+melee+mage), `COX_MAGIC_IS_DEFENSIVE_IDS`,
     `COX_USE_SINGLES_SCALING_IDS`, `SCAVENGER_BEAST_IDS`, `VESPINE_SOLDIER_IDS`,
     `DEATHLY_RANGER_IDS`.
   - (TEKTON/GUARDIAN/GLOWING_CRYSTAL/OLM hand sets, ARAXXOR/YAMA/VARDORVIS, ToA sets already present.)
2. **scaling/ChambersOfXericScaling.java** — `getSkillMeta`, `applySinglesCoxScaling`,
   `applyMultiCoxScaling`, `applyOlmScaling`, `applyCoxScaling` (XERICIAN gate). Uses
   `CalcMath.addPercent`/`iSqrt`. `max(offensives,...)` over the non-1 skills.
3. **scaling/TheatreOfBloodScaling.java** — hp-only: `TOB_MONSTER_IDS` `(party+3)/8`,
   `TOB_EM_MONSTER_IDS` ENTRY_MODE_SCALING factor table.
4. **scaling/TombsOfAmascutScaling.java** — hp-only invocation/path/party scaling + rounding;
   `TOA_WARDEN_CORE_EJECTED_IDS` hp=4500.
5. **scaling/VardorvisScaling.java** — `getVardNumbers` by version, `lerp(currHp,maxHp,0,start,end)`
   for str+def. `Number.isFinite` → guard (Java int currentHp always finite; if <=0 use maxHp? — match
   upstream: it uses inputs.monsterCurrentHp directly; INITIAL is 150 but sanitizeInputs resets to hp).
6. **scaling/Phases.java** — `applyMonsterPhases`: Araxxor Enraged def+35/magic+28/ranged+31;
   Yama mdef 60/-30 (defensive.magic). Guard `!inputs.phase`.
7. **MonsterScaling.java** — wire full ORDER_OF_OPERATIONS (cox,tob,toa,vard,phases,defenceReductions);
   `scaleMonsterHpOnly` → Vardorvis applyVardScaling.

BaseCalc constructor already calls `MonsterScaling.scaleMonster` — no change there.

## Part 2 — spec attacks (`getSpecCalc`/`getSpecDps`)

The spec damage/accuracy branches (the "spec block") are already ported. Need:

1. **Utils.getCombatStylesForCategory** — port the full switch (src/utils.ts:602-817) returning
   `List<PlayerCombatStyle>` (+ the appended Manual-Cast pseudo style). Needed by the sanitizeInputs
   spec branch.
2. **BaseCalc.sanitizeInputs** — replace the `TODO(M3)` throw with the faithful spec branch
   (BaseCalc.ts:742-758): if stance == Manual Cast → reset style to category[0], spell null; if weapon
   is Accursed sceptre (a)/Eldritch/Volatile nightmare staff → POWERED_STAFF[0], spell null.
3. **Equipment.WEAPON_SPEC_COSTS** + `Constants.UNDERWATER_MONSTERS` + `UNIMPLEMENTED_SPECS`/
   `PARTIALLY_IMPLEMENTED_SPECS` lists (in PlayerVsNpcCalc).
4. **PlayerVsNpcCalc.getSpecCost / isSpecSupported / getSpecCalc** (rebuild from baseMonster with
   `usingSpecialAttack=true`, thread repos through; null if unsupported). Add the
   `EQUIPMENT_SPEC_UNSUPPORTED` addIssue in the constructor (mirrors upstream).
5. **PlayerVsNpcCalc.getSpecDps** (:2421): Soulreaper-axe stacks path; else specCost path with
   Lightbearer 25 vs 50 tick regen. (`scaleMonsterHpOnly` ported in Part 1; `distAtHp`/
   `getTtkDistribution` remain not-needed — corpus ttk uses getTtk().)

## Part 3 — parity-verify (the gate)

1. **gen-parity-sweep.ts** — extend with:
   - **Scaling rows** (~20-40): ToA monsters at several invocation/party; CoX monsters incl. CM at
     party sizes; ToB monsters; Vardorvis. Set `monster.inputs` (partySize, toaInvocationLevel,
     toaPathLevel, partyMaxCombatLevel, partySumMiningLevel, partyMaxHpLevel, monsterCurrentHp,
     isFromCoxCm, phase). Assert maxHit/accuracy/dps (scaled).
   - **Spec rows** (~15-30): `usingSpecialAttack=true` for DWH, BGS, AGS, ZGS, dragon claws, dragon
     dagger, Voidwaker, abyssal dagger, etc. Assert specMaxHit/specAccuracy/specDps via getSpecCalc()/
     getSpecDps(). Add `spec:true` discriminator.
   - Self-check a known scaled value + a known spec value before emitting.
2. **CorpusRow / ScenarioPlayer** — extend `MonsterInputsInput` with partySize/party*/monsterCurrentHp/
   toaPathLevel/isFromCoxCm/phase; add `player.buffs.soulreaperStacks` + Lightbearer handling; add a
   top-level `spec` flag.
3. **ParityCorpusTest** — for `spec:true` rows, build calc.getSpecCalc() and map
   specMaxHit→getMax / specAccuracy→getHitChance / specDps→getSpecDps.
4. **gen-reference-corpus.ts** — allow the new expected fields + spec flag + new input fields.
5. **PORT_STATE.md** + **port-state.json** — scaling/* + `PlayerVsNpcCalc.spec` → ported.

## Gate
`./gradlew build --no-daemon --max-workers=1` green; new scaling+spec rows pass (exact/1e-9); all 406
existing rows still pass; PortStatusTest green; determinism OK; jar <10 MiB. Commit logically; no push.

## Anti-trap
If a generated row can't be reproduced (genuinely unported sub-feature), drop or honest-skip-tag it and
REPORT — do not fake. Likely candidates: Soulreaper-axe spec (needs stacks), partial specs (Ancient gs).
