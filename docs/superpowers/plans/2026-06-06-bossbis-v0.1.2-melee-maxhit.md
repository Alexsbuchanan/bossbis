# BossBiS v0.1.2 — Melee Max Hit Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** Port the player **melee max hit** (`getPlayerMaxMeleeHit`) and the min/max dispatch (`getMinAndMax`), exposing a `maxHit` the parity corpus can assert, so the **4 seeded melee `maxHit` rows light up and pass exactly** (Osmumten's fang 50, dragon-hunter-lance 58, blisterwood-flail 55, obsidian 46). Ranged/magic max hit stay stubbed (v0.1.3); distribution/DPS/TTK stay stubbed (v0.1.4+).

**Architecture:** Extend the existing `PlayerVsNpcCalc` (v0.1.1) — fill in `getPlayerMaxMeleeHit`, `getMinAndMax` (melee branch live; ranged/magic dispatch to their still-stubbed methods), and a `getMaxHit()` the corpus runner maps `maxHit` → . The `trackMaxHitFromEffective` helper already exists on `BaseCalc` (v0.1.1 M2). Gate is the parity corpus (real weirdgloop values).

**Tech Stack / conventions:** identical to the v0.1.1 plan (`docs/superpowers/plans/2026-06-06-bossbis-v0.1.1-accuracy-pipeline.md`) — read its "How to read this plan (PORT)" + "Global rules" sections; they apply verbatim (PORT-style, `strictfp`, **no RNG**, `long` in `trackFactor`, `Math.trunc`→`(int)`/`(long)`, faithful-first, per-task TDD + commit `port: … <weirdgloop@5e313c1>`, no push). JDK 17 → Java 11. Upstream source at `tools/upstream/src/`.

**Working directory:** `/home/alexb/runelite/projects/bossbis` (on `main` at tag `v0.1.1`). **Create a feature branch `feat/v0.1.2-melee-maxhit` first** (mirrors v0.1.1; `main` stays at the green `v0.1.1` tag until merged via PR).

**Parity tolerance:** `maxHit` is an integer → **exact**.

**Definition of done (all true):**
- `./gradlew build` green; the **4 melee `maxHit` corpus rows pass exactly** (fang 50, DHL 58, blisterwood 55, obsidian 46); the 2 **magic** `maxHit` rows still SKIP (magic max hit is v0.1.3); the 8 v0.1.1 rows (attack roll / defence roll / hit chance) still pass; `PortStatusTest` green.
- Determinism guard OK; jar < 10 MiB.
- `PORT_STATE.md` + `port-state.json`: `PlayerVsNpcCalc.maxMeleeHit` → `ported`; `maxRangedHit`/`maxMagicHit`/distribution/DPS still `not-ported`.
- CI green on `main` after PR merge; tag `v0.1.2`.

---

# Milestone 1 — Melee max hit

## Task 1.1: `getPlayerMaxMeleeHit` + `getMinAndMax` + `getMaxHit`

**Files:** Modify `src/main/java/org/bossbis/calc/PlayerVsNpcCalc.java`; Test `src/test/java/org/bossbis/calc/MeleeMaxHitTest.java`.

**Source (port faithfully, in order):** `tools/upstream/src/lib/PlayerVsNPCCalc.ts`
- `getPlayerMaxMeleeHit` (`:342-470+`): base str = `skills.str + boosts.str`; for each combat prayer apply `factorStrength` (with the `Burst of Strength ≤20` special `+1` case at `:350`); Soulreaper-axe stacks bonus (`:357-362`); stance bonus (`+8`, Aggressive `+3`, Controlled `+1`); melee void `[11,10]`; `gearBonus = bonuses.str + 64`; `baseMax = trackMaxHitFromEffective(effLevel, gearBonus)` (helper already on `BaseCalc`); then the **ordered damage multipliers** exactly as source: Crystal blessing (helm/legs/body pieces), the don't-stack group (avarice/forinthry, salve(e)/(ei) `[6,5]`, salve/(i) `[7,6]`, black mask on task `[7,6]`), Arclight/Emberlight demonbane, Bone/Burning claws demonbane, Obsidian (`baseMax × [1,10]` added), Dragon hunter lance `[6,5]` / wand `[7,5]` vs dragon, Keris vs kalphite (`[133,100]` / amascut `[115,100]`), Barronite/Granite golembane, rev weapon `[3,2]`, Silverlight/Darklight demonbane, Infernal tecpatl, Leaf-bladed `[47,40]`, Colossal blade (`+min(size*2,10)`), ratbone (`+10`), Inquisitor crush set, and any remaining branches through the end of the method (Dharok's HP-based, Dinh's, corpbane, etc.). Returns `MinMax`.
- `getMinAndMax` (`:1163-1200`): ammo-invalid → `[0,0]`; `overrides.maxHit`; dispatch by style (melee → `getPlayerMaxMeleeHit`; ranged/magic → their methods — **leave those throwing** `UnsupportedOperationException`); clamp `min>max` and negatives; return `MinMax`.
- Add `public int getMaxHit()` = `getMinAndMax().max()` (the corpus's `maxHit` field maps here).

**Gotchas:** Reuse the v0.1.1 `isWearingX` predicates, `getCombatPrayers`, `demonbaneFactor`, `trackFactor`/`trackAdd`/`trackAddFactor`/`trackMaxHitFromEffective` (port `demonbaneFactor`/`getCombatPrayers` now if v0.1.1 didn't). `long` in `trackFactor`. Obsidian adds `trunc(baseMax/10)` (computed from `baseMax`, not the running `maxHit`). The don't-stack group is an `if/else if` chain — preserve it. Leagues branches: drop (spec §4.2), consistent with v0.1.1.

- [ ] **Step 1: Failing unit test** — a melee max-hit case with a known value (e.g. Osmumten's fang max melee → 50, the same setup as corpus row 001), built via a fixture / `ScenarioPlayer`:

```java
package org.bossbis.calc;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

class MeleeMaxHitTest
{
	@Test
	void osmumtensFangMaxMelee()
	{
		// Same loadout as parity corpus row 001-osmumtens-fang-max-melee (expected maxHit 50).
		PlayerVsNpcCalc calc = MeleeMaxHitFixtures.osmumtensFangMaxMelee();
		assertThat(calc.getMaxHit()).isEqualTo(50);
	}
}
```

- [ ] **Step 2:** Run → FAIL (`getPlayerMaxMeleeHit`/`getMaxHit` not implemented). 
- [ ] **Step 3:** Port `getPlayerMaxMeleeHit` + `getMinAndMax` + `getMaxHit` (+ `demonbaneFactor`/`getCombatPrayers` if missing). 
- [ ] **Step 4:** Run → PASS; `./gradlew build`; `bash tools/check-determinism.sh` → OK.
- [ ] **Step 5: Commit** `port: getPlayerMaxMeleeHit + getMinAndMax (melee) <weirdgloop@5e313c1>`; `PORT_STATE.md` `PlayerVsNpcCalc` → `partial: accuracy + melee max hit`.

## Task 1.2: Light up the melee `maxHit` corpus rows

**Files:** Modify `src/test/java/org/bossbis/calc/ParityCorpusTest.java` (map `maxHit` → `getMaxHit()` if not already), `src/test/resources/parity/port-state.json`, `src/test/java/org/bossbis/calc/PortStatusTest.java`.

**Contract:** Map the corpus `maxHit` field → `getMaxHit()` in the runner. Flip `PlayerVsNpcCalc.maxMeleeHit` → `ported` in `port-state.json` (the 4 melee rows are tagged `exercises: ["PlayerVsNpcCalc.maxMeleeHit"]`; the 2 magic rows are tagged `maxMagicHit` and stay not-ported). Update `PortStatusTest` counts: the 4 melee rows now RUN; the 2 magic `maxHit` rows still SKIP.

**THE GATE:** `./gradlew test --tests 'org.bossbis.calc.ParityCorpusTest'` → rows **001-osmumtens-fang (50)**, **002-dragon-hunter-lance (58)**, **003-blisterwood-flail (55)**, **004-obsidian (46)** PASS EXACTLY; the 2 magic `maxHit` rows SKIP; all 8 v0.1.1 rows still pass. If a value mismatches, the max-hit port is wrong — fix it (re-check effective-strength order, the multiplier order, Obsidian-from-baseMax, the don't-stack group). Do NOT change expected values; they are real upstream assertions. If a row needs the corpus schema to carry something it doesn't (it shouldn't — these are plain melee), report it rather than fudging.

- [ ] **Steps 1–3:** Wire the mapping + flip port-state + update `PortStatusTest`; run the corpus; fix the port until the 4 rows pass exactly. — [ ] **Step 4: Commit** `test: melee max-hit corpus rows live (4 rows green) <weirdgloop@5e313c1>`.

---

# Milestone 2 — Broaden melee max-hit coverage

## Task 2.1: Add coverage rows for the damage special cases

**Files:** add `tools/scenarios/0NN-*.json`; regenerate the corpus; `PortStatusTest` counts.
**Why:** the 4 seeded rows exercise base max + dragon-hunter + vampyrebane + obsidian, but NOT salve / black-mask-on-task / inquisitor-crush / keris — all ported in Task 1.1 but unverified. Add rows so a wrong multiplier can't hide.
**Source:** transcribe from `tools/upstream/src/tests/calc/` — e.g. `Inquisitors.test.ts` (inquisitor crush `maxHit`), a salve(e)-vs-undead `maxHit` case (`GeneratedTests.test.ts` has "Osmumten's fang with salve in max melee" vs Vorkath — undead), a black-mask-on-task case, a keris-vs-kalphite case. Each: `exercises: ["PlayerVsNpcCalc.maxMeleeHit"]`, `expected: {"maxHit": <value>}`, full gear/skills/style + monster `{id, version}` (+ `inputs` if the case needs e.g. ToA for keris-of-the-sun — prefer plain cases). Transcribe expected values verbatim from the upstream test's `expect(maxHit).toBe(...)`; if a needed value isn't asserted at full precision, skip that case (max hit is an integer, so upstream asserts it exactly — transcription should be clean).

**Gate:** the new rows PASS exactly; if any fails, fix the corresponding multiplier branch in `getPlayerMaxMeleeHit` (this is the coverage payoff). `PortStatusTest` updated.

- [ ] **Steps 1–3:** Add ≥3 coverage rows (salve, black-mask/slayer, inquisitor, ideally keris); regenerate; run → all pass; fix any branch that mismatches. — [ ] **Step 4: Commit** `test: melee max-hit coverage rows (salve/black-mask/inquisitor/keris) <weirdgloop@5e313c1>`.

---

## Final verification & integration

- [ ] `./gradlew build` green; `./gradlew test` → all melee `maxHit` rows (4 seeded + coverage) pass, 2 magic `maxHit` rows skip, 8 v0.1.1 rows pass, `PortStatusTest` green, unit tests green.
- [ ] `bash tools/check-determinism.sh` OK; jar < 10 MiB.
- [ ] `PORT_STATE.md` + `port-state.json`: `PlayerVsNpcCalc.maxMeleeHit` ported; `maxRangedHit`/`maxMagicHit`/distribution/DPS not-ported.
- [ ] PR `feat/v0.1.2-melee-maxhit` → `main`; CI green on PR; merge; CI green on `main`; tag `v0.1.2`; delete branch (mirror v0.1.1 integration).

## Out of scope (v0.1.3+)
- Ranged + magic max hit (`getPlayerMaxRangedHit`/`getPlayerMaxMagicHit`) — v0.1.3; the 2 magic `maxHit` rows light up then.
- `HitDist`/distribution/`getMax` (distribution top)/`getDps`/`getTtk`/`getSpecCalc` — v0.1.4+. (Note: for these melee cases the distribution top equals `getMinAndMax().max()`, so `getMaxHit()` is a faithful proxy now; v0.1.4 wires the real distribution.)
- Leagues damage talents; full raid stat-scaling (carried over from v0.1.1).
